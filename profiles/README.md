# Profiles

The installable profile authority is packaged under
`reposeal.resources.profiles`. Profiles are activated only by a
manifest selection or by an explicit `requires` edge from a selected profile.
