# Lifecycle policy

Start with `docs/ARCHITECTURE.md`. Record requirements in a Review, confirm
observable behavior in a Specification, and map implementation in a Plan.
Use only the eight lifecycle operations declared in `AGENTS.md`. Work in a
plan-owned worktree and stop at ready until delivery is explicitly requested.
