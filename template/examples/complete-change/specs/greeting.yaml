specification:
  schema_version: 1
  id: example-greeting/greeting
  version: 1
  status: approved
  implementation_authorized: true
  review:
    id: example-greeting
    clauses: [EXAMPLE-001]
  decisions: []
  plan: examples/complete-change/plans/greeting.md
  intent:
    - expose one deterministic greeting behavior
  inputs:
    - a non-empty validated display name
  outputs:
    - a greeting containing that exact display name
  invariants:
    - invalid empty names fail at the public boundary
  acceptance:
    - a public-boundary test covers one valid and one empty name
