"""Example application boundary; replace it with the repository's product code."""


def greeting(name: str) -> str:
    """Return the example application's public greeting."""

    return f"Hello, {name}!"
