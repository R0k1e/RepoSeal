# Safety policy

Preserve unrelated changes and local state. Do not stash, force-push, run
destructive reset or cleanup commands, or put credentials and machine-specific
paths in Git. Reproduce bugs through public boundaries. Test observable
contracts and remove replaced authorities instead of adding unexplained shims.
